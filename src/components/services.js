import { Card } from "primereact/card";

const Services = () => {
  return (
      <Card title="Per i prezzi, aggiungi le date" className={`shadow-8`}>
       
      </Card>
  );
};

export default Services;